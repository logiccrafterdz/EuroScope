# EuroScope - EUR/USD Expert Bot
# Core
python-dotenv>=1.0.0
httpx>=0.27.0
apscheduler>=3.10.4

# Telegram Bot
python-telegram-bot>=21.0
aiohttp>=3.9.0
websockets>=12.0

# AI/LLM
openai>=1.50.0
# chromadb removed — replaced by built-in SQLite FTS5 (zero deps)

# Data & Analysis
yfinance>=0.2.36
pandas>=2.2.0
numpy>=1.26.0
# numba removed — not used in codebase, saves ~500MB

# Database
aiosqlite>=0.20.0

# News & Sentiment
ddgs>=8.1.1
transformers>=4.40.0
# torch removed — ONNX Runtime handles inference, saves ~2GB
# optimum removed — only needed for model export, not runtime
onnxruntime>=1.17.0

# System Monitoring
psutil>=5.9.0
pycryptodome>=3.20.0

# Reporting
reportlab>=4.0.0
